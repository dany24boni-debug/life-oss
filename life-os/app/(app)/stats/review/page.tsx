import { ReviewScreen } from "./review-screen";

// /stats/review — il riepilogo settimanale (B2.5, run-03).

export const metadata = { title: "Riepilogo settimanale — LifeOS" };

export default function ReviewPage() {
  return <ReviewScreen />;
}
