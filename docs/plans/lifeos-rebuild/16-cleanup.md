# Prompt 16 — Cleanup: repo root move, CI revival, docs truth

STATUS: STUB — finalize from `01-blueprint.md` (B5) with chat-Claude before running.

## Goal

Move the app from life-os/ to the repository root, root-level CI workflow (lint, typecheck, test, build), add typecheck script, prune one-shot scripts, untrack .DS_Store, rewrite README to reflect reality, update CLAUDE.md with conventions and architecture map.

## Scope

IN: repository layout, .github/workflows, package.json scripts, docs. OUT: any behavior change (pure moves + docs); Vercel root setting change documented.

Estimated size: S. Recommended model: Fable/Opus Effort Max.

## To be completed at finalization

- Files expected to change
- Step-by-step approach
- Acceptance criteria
- Smoke-test checklist (real device)
