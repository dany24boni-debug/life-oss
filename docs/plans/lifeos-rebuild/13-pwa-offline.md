# Prompt 13 — PWA: service worker, offline, install UX

STATUS: STUB — finalize from `01-blueprint.md` (B5) with chat-Claude before running.

## Goal

App-shell service worker with versioned precache + runtime caching and a safe update flow (new-version toast, kill-switch documented); offline fallback; install prompts + iOS coaching sheet; both-theme splash/status colors.

## Scope

IN: service worker + registration, manifest updates, install UI. OUT: push handlers, background sync, notification logic.

Estimated size: M. Recommended model: Fable/Opus Effort Max.

## To be completed at finalization

- Files expected to change
- Step-by-step approach
- Acceptance criteria
- Smoke-test checklist (real device)
