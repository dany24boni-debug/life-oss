# Prompt 02 — Foundation: Ember design system + custom UI kit + /dev/ui playground

STATUS: STUB — finalize from `01-blueprint.md` (B5) with chat-Claude before running.

## Goal

Implement the Ember tokens (B4) and the complete custom component library (Button, Input, Textarea, Select, Checkbox, Radio, Switch, DatePicker, TimePicker, Calendar month+week, Modal, BottomSheet, Toast, Tabs, Progress, StatCard, ChartFrame, EmptyState, Skeleton, CommandPalette shell) in an isolated folder, showcased at /dev/ui in every state.

## Scope

IN: src/ui/ (new), tokens CSS, playground route registration (single minimal edit). OUT: any import into existing screens, any change to existing pages, native date/time/select elements.

Estimated size: L. Recommended model: Fable/Opus Effort Max.

NOTE: this prompt corresponds to Phase C of session 00 — check 99-session-report.md for the gate outcome before scheduling.

## To be completed at finalization

- Files expected to change
- Step-by-step approach
- Acceptance criteria
- Smoke-test checklist (real device)
